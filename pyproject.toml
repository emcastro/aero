[tool.black]
line-length = 120

[project]
name = "aero"
version = "0.0.0"
dependencies = [
    "requests>=2.25.1",
    "rasterio>=1.2.10",
    "click>=8.1.8"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["microzarr"]