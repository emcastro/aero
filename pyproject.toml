[tool.black]
line-length = 120

[project]
name = "aero"
version = "0.0.0"
dependencies = [
    "requests>=2.25.1",
    "rasterio>=1.2.10",
    "click>=8.1.8",
    "pyright>=1.1.400",   
    "micropython-stm32-stubs==1.20.post5"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
include = ["microzarr"]